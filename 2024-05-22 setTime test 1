처음 쓰는 것
setTimeout(T대문자) = 문자만 찍힌다. settimeout(t소문자) = syntax 틀 자체가 찍힌다.
이 syntax는 외워야 한다!

 1  //

setTimeout(function(){
  console.log('1. 고양이가 날라다닙니다.');
},1000);
직역 -> 스탑워치 (쓰고 싶은 것), 기다려야하는 시간);
1초를 쓰고 싶다면 1000을 써야 한다. ( 그냥 그게 룰이다... ) 

setTimeout(function(){
  console.log('2. 강아지가 날라다닙니다.');
},1000);

setTimeout(function(){
  console.log('3. 너구리가 날라다닙니다.');
},1000);

 1,2,3 모두 "1초 뒤 실행" 으로 설정 했으니,
전부 "1초 뒤 동시에 문구가 나오는" 실행이 되어야 할 것 같다!

결과 :
"1초 뒤 동시에" 까지는 예상대로 실행이 되었다. 
그러나, 문구는 1-2-3 순으로 나왔다. (절차를 지켰다.) -> 1초 뒤 동시에 콘솔에 찍혔다. 1-2-3 순으로.
일반 함수(동기 함수)한테는 새치기 한다. 본인들(비동기 함수)끼리는 또 절차를 지킨다.( 이 기능은 최근 변경된 기능이다. )
서열이 존재한다.  동기 함수 = 절차를 잘 지키는 착한 학생들. 비동기 함수 = 착한 동기 함수들을 괴롭히고,
새치기 하는 일진들. 웃긴 점은, 본인들 그룹 안에서는 서로 예의 있게 절차적이다. 
